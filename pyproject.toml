[project]
python -m pip install requests folium livereload
name = "space-api"
version = "0.1.0"
description = "Fetch live astronaut and ISS position data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "folium>=0.20.0",
    "livereload>=2.7.1",
    "requests>=2.33.1",
]
